export interface ProjectData {
    name: string
    slug: string
    info_url: string
    deployment_url: string
    repository_url: string
    excerpt: string
    description: string
    features: string[]
    technologies: string[]
}